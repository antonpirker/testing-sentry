[project]
name = "test"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "celery>=5.5.2",
    "ipdb>=0.13.13",
    "redis>=6.0.0",
    "sentry-sdk",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
